{
  "name": "Automation_workflow_secure",
  "nodes": [
    {
      "parameters": {
        "resource": "fileFolder",
        "returnAll": true,
        "filter": {
          "folderId": "={{ $env.INCOMING_FOLDER_ID }}"
        }
      },
      "name": "Search files and folders",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3
    },
    {
      "parameters": {
        "operation": "download",
        "fileId": "={{ $json.id }}"
      },
      "name": "Download File",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const fileName = $input.item.json.name || 'unknown';\nconst fileId = $input.item.json.id || '';\nconst isPdf = fileName.toLowerCase().endsWith('.pdf');\n\nif (!isPdf) {\n  return { json: { fileName, fileId, _skip: true, _reason: `Unsupported file type: ${fileName}` } };\n}\n\nconst binary = $input.item.binary || {};\nconst keys = Object.keys(binary);\n\nif (!keys.length) {\n  return { json: { fileName, fileId, _skip: true, _reason: 'No binary data' } };\n}\n\nreturn {\n  json: { fileName, fileId, _skip: false },\n  binary: { data: binary[keys[0]] }\n};"
      },
      "name": "Read File",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "pdf"
      },
      "name": "Extract PDF Text",
      "type": "n8n-nodes-base.extractFromFile",
      "typeVersion": 1
    },
    {
      "parameters": {
        "jsCode": "const input = $input.first().json;\nreturn [{ json: { text: input.text || '', fileName: input.fileName, fileId: input.fileId } }];"
      },
      "name": "Combine PDF Text",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "const input = $input.first().json;\nconst GEMINI_API_KEY = $env.GEMINI_API_KEY;\n\nif (!input.text || input.text.length < 10) {\n  return [{ json: { _skip: true, _reason: 'No readable text', source_file: input.fileName, file_id: input.fileId } }];\n}\n\nconst response = await this.helpers.httpRequest({\n  method: 'POST',\n  url: `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${GEMINI_API_KEY}`,\n  json: true,\n  body: {\n    contents: [{ parts: [{ text: input.text.substring(0, 1000) }] }]\n  }\n});\n\nconst raw = response.candidates?.[0]?.content?.parts?.[0]?.text || '{}';\nlet data;\ntry { data = JSON.parse(raw); } catch { return [{ json: { _skip: true, _reason: 'Invalid JSON', source_file: input.fileName, file_id: input.fileId } }]; }\n\nreturn [{ json: { ...data, source_file: input.fileName, file_id: input.fileId } }];"
      },
      "name": "Extract Invoice Fields (Gemini)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "move",
        "fileId": "={{ $json.fileId }}",
        "folderId": "={{ $env.PROCESSED_FOLDER_ID }}"
      },
      "name": "Move to Processed",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3
    },
    {
      "parameters": {
        "operation": "move",
        "fileId": "={{ $json.file_id }}",
        "folderId": "={{ $env.FAILED_FOLDER_ID }}"
      },
      "name": "Move to Failed",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3
    }
  ]
}